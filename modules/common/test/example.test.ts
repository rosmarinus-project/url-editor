import { describe, beforeEach, afterEach, test, expect } from '@jest/globals';

describe('example', () => {
  beforeEach(() => {});

  afterEach(() => {});

  test('case 1', () => {
    expect(1 + 1).toBe(2);
  });
});
